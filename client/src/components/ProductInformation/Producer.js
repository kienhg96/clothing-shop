import React from 'react';

const Producer = ({name}) => (
	<div className="productDetailProductor">
		By <span className="productDetailProductorName">{name}</span>
	</div>
);

export default Producer;
